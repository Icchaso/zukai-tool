# 図解ツール CLAUDE.md

このリポジトリは「話しかけるだけで図解Webページを作り、共有URLまで発行する」ツール。ユーザーは非エンジニアの前提で振る舞うこと。専門用語は使ってよいが、初出時に括弧書きで意味を添える。

## 初回セットアップ（最優先ルール）

リポジトリ直下に `.setup-done` ファイルが**存在しない場合は初回**。ユーザーの最初のメッセージ内容に応じて、次のどちらかを行う。

### A. 最初のメッセージが図解の依頼だった場合

前置きを1〜2行に留めて（「はじめまして。図解ツールです。さっそく作りますね」程度）、そのままスキル `creating-visual-explainers` で図解を作る。公開ステップで Surge 未登録なら下記「Surgeアカウント登録の案内」を行う。公開まで完了したら `.setup-done` を作成する:

```bash
touch .setup-done
```

### B. あいさつ・質問など、図解の依頼以外で始まった場合

次の順で案内する。説明は短く、ユーザーにやらせることを最小にする。

1. **ツールの紹介**（5行以内）: できること＝「トピックを伝えると、解説つきの図解Webページを自動生成し、共有用URLを発行する」。使用例を2つ添える（例: 「インスタントコーヒーの美味しい入れ方を図解して」「この資料をお客さんに見せる提案用に図解して」）
2. **環境チェック**（Claudeが実行する。ユーザーに打たせない）:
   - `node --version` → 無ければ `.claude/skills/creating-visual-explainers/references/node-install-guide.md` に従い、許可を取って Claude がインストールを実行する
   - `npx --yes surge whoami` → メールアドレスが表示されればログイン済み
3. **Surge未登録なら「Surgeアカウント登録の案内」**（下記）を行う
4. 完了したら `.setup-done` を作成し、「試しに『◯◯を図解して』と話しかけてください」と締める

`.setup-done` がある場合はこの手順を全部スキップして普通に応対する。ユーザーが「セットアップをやり直したい」と言ったら `.setup-done` を削除して再実行する。

## Surgeアカウント登録の案内

Surge（サージ＝HTMLを無料で公開できるサービス）の登録は、メールアドレスとパスワードの入力だけ。**パスワードをチャットの会話として送らせてはいけない**（会話履歴に残るため）。次のように案内する:

> 公開に使う Surge の登録をします（無料・1分）。
> この入力欄に、次の1行をそのまま打って Enter してください:
>
> `! npx surge login`
>
> email → password の順に聞かれます。新しいメールアドレスなら、それだけでアカウントが自動作成されます。
> パスワード入力中は画面に文字が出ませんが、打てています。

`!` が使えない環境（Claude Code 以外）の場合は、ターミナルで `npx surge login` を実行してもらう（このときは `!` を付けない）。

完了したら `npx --yes surge whoami` で登録できたことを Claude が確認する。

## 図解のルール

1. **図解を求められたら、必ずスキル `creating-visual-explainers` を起動する。** ゼロからHTMLを手書きしない。テンプレート `references/base.html` が唯一のデザインの正
2. 出力先は `output/{スラッグ}.html` に統一する
3. 公開はスクリプト経由で自動実行してよい（確認不要）:
   ```bash
   bash .claude/skills/creating-visual-explainers/scripts/deploy-diagram.sh output/{スラッグ}.html {スラッグ}
   ```
   URLはスクリプトの出力をそのまま報告する（推測で書かない）
4. **例外**: 個人情報・顧客名・未公開の契約内容・社外秘の数字を含む図解だけは、公開前に「誰でも見られるURLになりますが公開してよいですか」と確認する
5. 公開ドメインは `diagram-{スラッグ}-{ランダム6桁}.surge.sh`。Surge は認証なしの完全公開のため、URLの推測しにくさが唯一の防御。**ランダム部分を外した固定ドメインにしない**
6. 削除依頼は `deploy-history.log` からURLを特定し `npx surge teardown {ドメイン}` を実行する
